<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- L'échappatoire `windowOptOutEdgeToEdgeEnforcement` a été retirée : elle est
             DÉSACTIVÉE à partir de targetSdk 36 sur Android 16, donc elle ne servait plus qu'à
             donner une fausse impression de sécurité. L'edge-to-edge est désormais assumé :
             MainActivity encarte le contenu depuis les WindowInsets. -->
        <!-- Habille les bandes laissées libres sous les barres système (voir colors.xml). -->
        <item name="android:windowBackground">@color/tentacleInk</item>
        <!-- Interface sombre → icônes de la barre d'état en clair. -->
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>